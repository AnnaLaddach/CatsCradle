
library(Seurat)
library(pheatmap)

rm(list=ls())
graphics.off()

source('../CradleWare.R')

## ###################################################
## ###################################################

res = 1
for(assay in c('integrated','RNA'))
{
    objectPair = getObjectPair(assay,res)
    f = objectPair$f
    fPrime = objectPair$fPrime

    stop('checkpoint')
}
